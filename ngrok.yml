version: "2"
authtoken: # Your ngrok auth token will be used automatically
tunnels:
  client:
    addr: 3000
    proto: http
    host_header: localhost:3000
  server:
    addr: 3001
    proto: http
    host_header: localhost:3001
